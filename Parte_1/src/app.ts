export function sumar(a:number, b:number): number {
    let respuesta=0;
    respuesta=a+b;
    return respuesta;
}
// El lenguaje typescript sirve para 
// escribir código que lanza errores al instante a demas de
// que permmite detectar errores durante el desarrollo y asi 
// corregir este a demas de que desde la funcion se define
// el valor como va a ir expresado.