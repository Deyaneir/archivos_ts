/*let edad: number;
let nombre: string; 
let administrador: boolean;

edad=22;
nombre="Damaris";
administrador=true;*/
// Esto es si en caso de estar programando declaramos las variables 
/*let edad=22;
let nombre="Damaris";
let administrador=true;*/
// En este caso se declara las variables y se asigna el valor 
// al mismo tiempo, esto es mas practico.

let edad:number=22;
let nombre:string="Damaris";
let administrador:boolean=true;

let profecion: string|null;
profecion=null;
profecion="Desarrolladora";
// En este caso se declara la variable y se asigna el tipo de dato
// pero no se asigna un valor, esto es para cuando no se sabe el valor
// que va a tener la variable pero si se sabe el tipo de dato que va a ser,
// en este caso se asigna el valor null para indicar que no tiene un valor asignado.
let pais;
console.log(pais);
// En este caso se declara la variable y se asigna el tipo de dato
// pero no se asigna un valor, esto es para cuando no se sabe el valor
// que va a tener la variable pero si se sabe el tipo de dato que va a ser.